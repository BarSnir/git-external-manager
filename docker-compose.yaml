# docker-compose.yml
version: '3.8'

services:
  alpine_service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alpine_container
    restart: always
    command: ["./script.sh"]
    env_file:
      - .env
    volumes:
    - .:/repo